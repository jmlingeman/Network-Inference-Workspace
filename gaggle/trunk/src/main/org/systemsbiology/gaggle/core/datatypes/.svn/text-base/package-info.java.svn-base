/**
 * Contains the core datatypes of the Gaggle. These all implement the interface GaggleData
 * which in turn implements Serializable, which is required for all classes that are going
 * to be transmitted over RMI.
 */

package org.systemsbiology.gaggle.core.datatypes;